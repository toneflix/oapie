# Examples

This directory contains self-contained SDK examples that mirror the package shapes emitted by `oapie generate sdk`.

- `runtime-sdk`: demonstrates `-O runtime` output with a manifest-driven client entrypoint
- `both-sdk`: demonstrates `-O both` output with generated API classes plus the runtime bundle helper

Each package includes its own test and build config so it can be opened and run independently.
